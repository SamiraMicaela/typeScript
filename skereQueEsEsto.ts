//leer valores hasta que se ingrese 0. Se podra introducir valores positivos y negativos. Encontrar el maximo. Analizar como cambia el progreso para hallar el minimo.
import * as rls from "readline-sync";
let n : number = rls.questionInt ("Ingrese la cantidad de numeros:");
let pos : number = 1;
let neg : number = -1;
let neu : number = 0;

for (n >= -10000000; n <= 10000000; n ++){
    if (n >= 1 ){
      pos == pos +1;   
    console.log ("Este numero es positivo")
    } else {
        (n <= -1)
            neg == neg +1; 
        console.log("Este numero es negativo")
        
        
    } break
    
    
}